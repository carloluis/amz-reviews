// Not implemented yet!

import React from 'react';
import Select from '../select/Select';

const GROUPS = ['', 'Day', 'Week', 'Month'];

const GroupBy = () => <Select onSelect={() => {}} title="Group by" values={GROUPS} disabled />;

export default GroupBy;
